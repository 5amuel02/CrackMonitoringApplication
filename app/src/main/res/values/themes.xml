<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SeismicAplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>

        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!--
            ✅ FIX: Override style BottomNavigationView global
            Ini mencegah MaterialComponents inject background putih (colorSurface)
            ke setiap item icon di BottomNavigationView
        -->
        <item name="bottomNavigationStyle">@style/CustomBottomNavStyle</item>

    </style>

    <!--
        Style BottomNav custom:
        - itemBackground transparan → tidak ada kotak putih di tiap icon
        - itemRippleColor transparan → tidak ada ripple putih saat di-tap
        - android:background null → background bar ikut warna yang di-set programatik
    -->
    <style name="CustomBottomNavStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemBackground">@android:color/transparent</item>
        <item name="itemRippleColor">@android:color/transparent</item>
        <item name="android:background">@null</item>
    </style>

</resources>